<!DOCTYPE html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>

<body>

    {% for name in names %}
        {% for color in colors %}
            <h1 style="background-color:{{ color }};">{{ name }}</h1>
        {% endfor %}
    {% endfor %}

</body>
</html>